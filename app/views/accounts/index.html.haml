%h1 Listing accounts

%table
  %thead
    %tr
      %th Status
      %th Account tipe
      %th Service
      %th Booking
      %th User
      %th
      %th
      %th

  %tbody
    - @accounts.each do |account|
      %tr
        %td= account.status
        %td= account.account_tipe
        %td= account.service
        %td= account.booking_id
        %td= account.user_id
        %td= link_to 'Show', account
        %td= link_to 'Edit', edit_account_path(account)
        %td= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Account', new_account_path
